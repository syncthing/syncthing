syntax = "proto3";

package config;

import "repos/protobuf/gogoproto/gogo.proto";

enum Tuning {
    option (gogoproto.goproto_enum_stringer) = false;

    TUNING_AUTO  = 0;
    TUNING_SMALL = 1;
    TUNING_LARGE = 2;
}
